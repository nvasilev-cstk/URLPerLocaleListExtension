{
  "name": "locale-url-list-monitor",
  "private": true,
  "version": "0.1.0",
  "description": "Contentstack App: aggregates the localized `url` field of an entry into Entry Metadata as a single locale -> URL map, fetchable via one CDA call.",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "preview": "vite preview --port 3000"
  },
  "dependencies": {
    "@contentstack/app-sdk": "^1.6.0",
    "axios": "^1.7.7",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
